# Copy to .env. Keys are read from the environment; nothing is hardcoded.
ANTHROPIC_API_KEY=            # debrief + LLM fallback classifier (Claude)
GEMINI_API_KEY=               # synthetic prospect voice (Gemini Live)
# COACH_STT_MODEL=mlx-community/whisper-small.en-mlx   # local STT for human seats (offline)
# COACH_LLM_MODEL=claude-opus-5
# PUBLIC_HOST=                # tunnel host for Twilio webhooks
